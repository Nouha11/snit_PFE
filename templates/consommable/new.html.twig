{% extends 'base.html.twig' %}

{% block title %}Nouveau consommable{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/consommable.css') }}?v={{ "now"|date("U") }}">
{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="card shadow">
            <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
                <h1 class="h3 mb-0">Créer un nouveau consommable</h1>
                <a href="{{ path('app_consommable_index') }}" class="btn btn-light">
                    <i class="fas fa-arrow-left me-1"></i> Retour
                </a>
            </div>
            
            <div class="card-body">
                {{ include('consommable/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}