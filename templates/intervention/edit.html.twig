{% extends 'base.html.twig' %}

{% block title %}Modifier l'intervention{% endblock %}

{% block body %}
<div class="container mt-5">
    <nav aria-label="breadcrumb" class="mb-4">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('app_intervention_index') }}">Interventions</a></li>
            <li class="breadcrumb-item"><a href="{{ path('app_intervention_show', {'id': intervention.id}) }}">Détails #{{ intervention.id }}</a></li>
            <li class="breadcrumb-item active" aria-current="page">Modifier</li>
        </ol>
    </nav>

    <div class="row">
        <div class="col-lg-8">
            <div class="card shadow-sm border-0 mb-4">
                <div class="card-header bg-primary bg-gradient text-white">
                    <h1 class="h3 mb-0">
                        <i class="bi bi-pencil-square me-2"></i>Modifier l'intervention
                    </h1>
                </div>
                
                <div class="card-body">
                    {{ include('intervention/_form.html.twig', {
                        'button_label': 'Enregistrer les modifications',
                        'button_class': 'btn-primary'
                    }) }}
                </div>
            </div>
        </div>
        
        <div class="col-lg-4">
            <div class="card shadow-sm border-0 mb-4">
                <div class="card-header bg-light">
                    <h5 class="mb-0">Actions</h5>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a href="{{ path('app_intervention_show', {'id': intervention.id}) }}" class="btn btn-outline-secondary">
                            <i class="bi bi-arrow-left me-1"></i> Retour aux détails
                        </a>
                        <a href="{{ path('app_intervention_index') }}" class="btn btn-outline-secondary">
                            <i class="bi bi-list me-1"></i> Retour à la liste
                        </a>
                        <button type="button" class="btn btn-outline-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
                            <i class="bi bi-trash me-1"></i> Supprimer cette intervention
                        </button>
                    </div>
                </div>
            </div>
            
            <div class="card shadow-sm border-0">
                <div class="card-header bg-light">
                    <h5 class="mb-0">Conseils</h5>
                </div>
                <div class="card-body">
                    <ul class="small text-muted mb-0 ps-3">
                        <li>Tous les champs marqués d'un * sont obligatoires</li>
                        <li>Vérifiez le type d'intervention avant de soumettre</li>
                        <li>La date doit être au format JJ/MM/AAAA</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Delete Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title" id="deleteModalLabel">Confirmer la suppression</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="alert alert-warning">
                    <i class="bi bi-exclamation-triangle me-2"></i>
                    <strong>Attention :</strong> Vous êtes sur le point de supprimer cette intervention.
                </div>
                <p>Cette action est irréversible. Êtes-vous certain de vouloir continuer ?</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
                {{ include('intervention/_delete_form.html.twig', {'button_class': 'btn-danger'}) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}